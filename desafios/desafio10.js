/*Faça um programa que dado uma lista de nomes imprima os nomes que começem com a letra "V". */
const nomes = ['vando','felipe','vanessa','luana','Vanusa'];
for (let i=0;i<nomes.length;i++){
    if ([0] ===v){
        console.log(nomes);
    }
}